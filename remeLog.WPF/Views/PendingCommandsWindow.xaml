<Window x:Class="remeLog.Views.PendingCommandsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:remeLog.ViewModels"
        mc:Ignorable="d" WindowStartupLocation="CenterOwner"
        Title="Очередь задач" Height="450" Width="800">
    <!-- DataContext задаётся в code-behind: вьюмодели нужен фильтр по приложению. -->
    <DockPanel>
        <Border DockPanel.Dock="Top" Padding="8,4" BorderThickness="0,0,0,1" BorderBrush="LightGray">
            <StackPanel Orientation="Horizontal">
                <Button Content="Закрыть" Command="{Binding CloseCommand}" Padding="8,2"/>
                <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="8,0"/>
                <TextBlock VerticalAlignment="Center">
                    <Run Text="Команд в очереди:"/>
                    <Run FontWeight="Bold" Text="{Binding Commands.Count, Mode=OneWay}"/>
                </TextBlock>
            </StackPanel>
        </Border>
        <ProgressBar DockPanel.Dock="Bottom" Height="2"
                     IsIndeterminate="True"
                     Visibility="{Binding InProgress, Converter={StaticResource VisibilityConverter}}"/>
        <ListView ItemsSource="{Binding Commands}"
                  FontSize="14" Margin="8">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Время" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding CreatedUtc, StringFormat={}{0:dd.MM HH:mm}}"
                                           VerticalAlignment="Center"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Тип" DisplayMemberBinding="{Binding CommandType}" Width="120"/>
                    <GridViewColumn Header="Приложение" DisplayMemberBinding="{Binding TargetApplication}" Width="90"/>
                    <GridViewColumn Header="Получатель" DisplayMemberBinding="{Binding RecipientDisplay}" Width="150"/>
                    <GridViewColumn Header="Команда" DisplayMemberBinding="{Binding Payload}" Width="200"/>
                    <GridViewColumn Header="" Width="80">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Отменить"
                                        Command="{Binding DataContext.CancelCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        CommandParameter="{Binding Id}"
                                        Padding="4,1" FontSize="12"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</Window>
