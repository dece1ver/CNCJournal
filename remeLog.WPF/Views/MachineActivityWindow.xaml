<Window x:Class="remeLog.Views.MachineActivityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:cnt="clr-namespace:remeLog.Views.Controls"
        xmlns:vm="clr-namespace:remeLog.ViewModels"
        mc:Ignorable="d" WindowStartupLocation="CenterOwner"
        Title="Сейчас на станках" Height="450" Width="1200"
        SizeChanged="Window_SizeChanged">
    <Window.DataContext>
        <vm:MachineActivityViewModel/>
    </Window.DataContext>
    <DockPanel>
        <ProgressBar DockPanel.Dock="Bottom" Height="2"
                     IsIndeterminate="True"
                     Visibility="{Binding InProgress, Converter={StaticResource VisibilityConverter}}"/>
        <Grid>
            <ListView x:Name="RowsListView" ItemsSource="{Binding Rows}" FontSize="14" Margin="8">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="ToolTip"
                                Value="{Binding Activity.UpdatedLocal, StringFormat=Обновлено: {0:dd.MM.yyyy HH:mm:ss}}"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Станок" DisplayMemberBinding="{Binding Machine}" Width="110"/>
                        <GridViewColumn Header="Статус" Width="260">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Activity, Converter={StaticResource MachineActivityStatusTextConverter}}"
                                               Foreground="{Binding Activity, Converter={StaticResource MachineActivityStatusBrushConverter}}"
                                               FontWeight="Bold"
                                               TextTrimming="CharacterEllipsis"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Деталь" DisplayMemberBinding="{Binding Activity.PartName}" Width="220"/>
                        <GridViewColumn Header="Установ" DisplayMemberBinding="{Binding Activity.Setup}" Width="70"/>
                        <GridViewColumn Header="Заказ" DisplayMemberBinding="{Binding Activity.Order}" Width="90"/>
                        <GridViewColumn Header="Начало фазы" DisplayMemberBinding="{Binding Activity.PhaseStartLocal, StringFormat={}{0:dd.MM HH:mm:ss}}" Width="130"/>
                        <GridViewColumn x:Name="OperatorColumn" Header="Оператор" DisplayMemberBinding="{Binding Activity.Operator}" Width="150"/>
                    </GridView>
                </ListView.View>
            </ListView>
            <cnt:LoadingOverlay IsBusy="{Binding InProgress}"/>
        </Grid>
    </DockPanel>
</Window>
