<Window x:Class="remeLog.Views.MachineActivityWindow" chrome:WindowChromeTheme.Enabled="True"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:chrome="clr-namespace:libeLog.Infrastructure;assembly=libeLog"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:cnt="clr-namespace:remeLog.Views.Controls"
        xmlns:vm="clr-namespace:remeLog.ViewModels"
        mc:Ignorable="d" WindowStartupLocation="CenterOwner"
        Title="Сейчас в работе" Height="450" Width="1300"
        Background="{DynamicResource Areopag.Bg0}">
    <Window.DataContext>
        <vm:MachineActivityViewModel/>
    </Window.DataContext>
    <DockPanel>
        <ProgressBar DockPanel.Dock="Bottom" Height="2"
                     IsIndeterminate="True"
                     Visibility="{Binding InProgress, Converter={StaticResource VisibilityConverter}}"/>
        <Grid>
            <DataGrid ItemsSource="{Binding Rows}" AutoGenerateColumns="False" IsReadOnly="True"
                      VerticalGridLinesBrush="{DynamicResource Areopag.LineSoft}" HorizontalGridLinesBrush="{DynamicResource Areopag.LineSoft}"
                      ColumnWidth="SizeToCells" CanUserSortColumns="False"
                      VerticalScrollBarVisibility="Visible"
                      SelectionUnit="CellOrRowHeader" CanUserReorderColumns="False"
                      SelectionMode="Extended" EnableColumnVirtualization="True"
                      EnableRowVirtualization="True"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                      Margin="0 -1 0 0" BorderThickness="0"
                      FontSize="14">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow" BasedOn="{StaticResource Areopag.DataGridRow}">
                        <Setter Property="ToolTip"
                                Value="{Binding Activity.UpdatedLocal, StringFormat=Обновлено: {0:dd.MM.yyyy HH:mm:ss}}"/>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Resources>
                    <!-- Остальное (фон, рамка, шрифт, шаблон с грипперами) — из глобального Areopag.DataGridColumnHeader -->
                    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource Areopag.DataGridColumnHeader}">
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="Padding" Value="8 5"/>
                        <Setter Property="MinHeight" Value="32"/>
                        <Setter Property="TextOptions.TextRenderingMode" Value="Grayscale"/>
                        <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Станок" Binding="{Binding Machine}" Width="200">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource TableTextBlock}">
                                <Setter Property="TextAlignment" Value="Left"/>
                                <Setter Property="Margin" Value="4 0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTemplateColumn Header="Статус" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Activity, Converter={StaticResource MachineActivityStatusTextConverter}}"
                                           Foreground="{Binding Activity, Converter={StaticResource MachineActivityStatusBrushConverter}}"
                                           Style="{StaticResource TableTextBlock}"
                                           FontWeight="Bold"
                                           TextTrimming="CharacterEllipsis"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Деталь" Binding="{Binding Activity.PartName}" Width="250">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource TableTextBlock}">
                                <Setter Property="TextAlignment" Value="Left"/>
                                <Setter Property="Margin" Value="4 0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Установ" Binding="{Binding Activity.Setup}" Width="60">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource TableTextBlock}"/>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Заказ" Binding="{Binding Activity.Order}" Width="150">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource TableTextBlock}"/>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Начало фазы" Binding="{Binding Activity.PhaseStartLocal, StringFormat={}{0:dd.MM HH:mm:ss}}" Width="130">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource TableTextBlock}"/>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Оператор" Binding="{Binding Activity.Operator}" Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource TableTextBlock}">
                                <Setter Property="TextAlignment" Value="Left"/>
                                <Setter Property="Margin" Value="4 0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
            <cnt:LoadingOverlay IsBusy="{Binding InProgress}"/>
        </Grid>
    </DockPanel>
</Window>
